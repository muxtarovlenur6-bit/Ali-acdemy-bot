[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements-prod.txt"]

[variables]
PYTHON_DISABLE_ENSUREPIP = "1"

[start]
cmd = "python run.py"
