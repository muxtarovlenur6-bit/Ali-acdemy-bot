[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python -m ensurepip --upgrade", "python -m pip install -r requirements-prod.txt"]

[environment]
PYTHON_DISABLE_ENSUREPIP = "1"

[start]
cmd = "python run.py"
